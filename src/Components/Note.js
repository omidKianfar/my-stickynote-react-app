import React, { Children } from "react";

const Note = () => {
  return (
    <div>
      <p>Note</p>
      <button>Edit</button>
      <button>Delete</button>
    </div>
  );
};

export default Note;
